spring:
  application:
    name:
      matchmaking-service
  cloud:
    config:
      enabled: true
      fail-fast: true
      label: master
  config:
    import: optional:config-service:http://localhost:8888/
  profiles:
    active: dev

eureka:
  client:
    service-url:
      uris: http://localhost:8761/eureka/
      defaultZone: http://localhost:8761/eureka/
    healthcheck:
      enabled: true
    enabled: true
  instance:
    secure-port-enabled: false
    non-secure-port-enabled: true
    prefer-ip-address: true
    instance-id: ${spring.application.name}